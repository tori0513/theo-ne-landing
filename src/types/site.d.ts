/** Type surface for scripts/site.mjs, aliased as `@site` (see vite.config.ts). */
declare module '@site' {
  export const ORIGIN: string;
  export const LINKS: {
    linkedin: string;
    trops: string;
    teheranroai: string;
  };
  export const LANGS: string[];
  export const DEFAULT_LANG: string;
}
